#ifndef MENU_H
#define MENU_H

//Function for the menu gamestate that runs a graphical interface for the user to navigate through.
int menuStart(char ipAddress[]);
//Function to display a gameover window that shows the final score.
void gameOverMenu(int last_SpawnTime);

#endif